-- =============================================
-- V3: Add updated_at column (이미 V1에서 모든 테이블에 추가됨)
-- 이 파일은 V1에서 누락된 테이블만 처리
-- =============================================

-- V1에서 누락된 테이블들만 추가
-- (V1에서 이미 대부분 테이블에 updated_at이 있으므로 추가 작업 없음)
