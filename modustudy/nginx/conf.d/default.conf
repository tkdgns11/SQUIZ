# ===========================================
# Upstream 정의
# ===========================================
upstream backend {
    server backend:8080;
}

upstream sfu {
    server sfu-server:3000;
}

upstream recorder {
    server recorder:3001;
}

# ===========================================
# HTTP Server (80 → HTTPS 리다이렉트용, 개발 시 직접 사용)
# ===========================================
server {
    listen 80;
    server_name localhost;

    # =========================================
    # Frontend (React 정적 파일)
    # =========================================
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
    }

    # =========================================
    # Backend API
    # =========================================
    location /api/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # =========================================
    # Backend WebSocket (채팅, 알림)
    # =========================================
    location /ws/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }

    # =========================================
    # SFU Server (Mediasoup 시그널링)
    # =========================================
    location /sfu/ {
        proxy_pass http://sfu;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }

    # =========================================
    # Recorder Server
    # =========================================
    location /recorder/ {
        proxy_pass http://recorder;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # =========================================
    # Health Check
    # =========================================
    location /health {
        access_log off;
        return 200 "OK";
        add_header Content-Type text/plain;
    }
}

# ===========================================
# HTTPS Server (운영 환경 - SSL 인증서 필요)
# ===========================================
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     # SSL 설정
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#
#     # 위와 동일한 location 블록들...
# }
