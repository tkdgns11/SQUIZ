# ===========================================
# Upstream Configuration (단일 환경)
# ===========================================

upstream backend {
    server squiz-backend:8080;
}

upstream sfu {
    server squiz-sfu:4000;
}

upstream frontend {
    server squiz-frontend:80;
}

upstream cs-quiz-ai {
    server squiz-cs-quiz-ai:5000;
}

upstream monitor {
    server squiz-monitor:9090;
}
