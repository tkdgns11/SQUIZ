server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/conference?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ssafy
    password: ssafy
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

app:
  cors:
    allowed-origins: "*"
  sfu:
    base-url: "wss://localhost:4000"
